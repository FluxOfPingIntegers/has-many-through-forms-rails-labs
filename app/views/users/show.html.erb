<h1><%= @user.username %></h1>
<h3><%= @user.email %></h3>

This User has commented on the following Posts:
<ul>
  <% @user.comments.each do |c| %>
    <li>On <%= link_to c.post.title, c.post %> This User Commented: <%= c.content %></li>
  <% end %>
</ul>
