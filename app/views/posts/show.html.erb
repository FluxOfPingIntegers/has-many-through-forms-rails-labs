<h1><%= @post.title %></h1>

<p><%= @post.content %></p>

This Post is listed under the following Categories:
<ul>
  <% @post.categories.each do |c| %>
    <li><%= link_to c.name, c %></li>
  <% end %>
</ul>

The following Users have Commented on this Post:
<ul>
  <%= @post.users.uniq.each do |u| %>
    <li><%= link_to u.username, u %></li>
  <% end %>
</ul>

<h3>Here are all of the users comments that have been made on this post:</h3>
<ul>
  <% @post.comments.each do |comment| %>
    <li><%= comment.user.username %> says: <%= comment.content %></li>
  <% end %>
</ul>

<%= form_for @post.comments.build do |f| %>
  <%= f.label "Content" %><br>
  <%= f.text_area :content %><br>
  <%= f.hidden_field :post_id, value: @post.id %>
  <%= f.label "Select Existing User(s)" %><br>
  <%= f.collection_select :user_id, User.all, :id, :username %><br>
  <%= f.label "Or Create A New User" %><br>
  <%= f.fields_for :user_attributes do |u| %>
    <%= u.label "Username" %><br>
    <%= u.text_field :username %><br>
  <% end %>

  <%= f.submit %>
<% end %>
